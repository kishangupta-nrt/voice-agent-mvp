{
  "name": "voice-agent-mvp",
  "version": "2.0.0",
  "private": true,
  "description": "Voice AI Agent with JWT Auth",
  "scripts": {
    "install:server": "cd server && npm install",
    "build:server": "cd server && npm run build",
    "start:server": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}